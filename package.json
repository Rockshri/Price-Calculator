{
  "name": "price-calculator",
  "version": "1.0.0",
  "engines": { "node": "20.x" },
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
